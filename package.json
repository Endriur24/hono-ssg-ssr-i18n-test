{
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "wrangler pages dev",
    "deploy": "vite build && wrangler pages deploy ./dist --commit-dirty=true"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "@hono/vite-dev-server": "^0.19.0",
    "@hono/vite-ssg": "^0.1.1",
    "vite": "^6.2.1",
    "wrangler": "^3.114.0"
  }
}
